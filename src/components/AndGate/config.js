import {transform} from '../../constants'
import {signal}    from '../../constants'

const color    = 'yellow'
const width1   = 14
const width2   = 1.75*width1
const velocity = 8

const config = {
  color,
  width1,
  width2,
  velocity,
  transform,
  signal
}


export {config}